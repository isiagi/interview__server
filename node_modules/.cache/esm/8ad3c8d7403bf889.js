let nodemailer,smtpTransport,dotenv;_403‍.x([["default",()=>_403‍.o]]);_403‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);_403‍.w("nodemailer-smtp-transport",[["default",["smtpTransport"],function(v){smtpTransport=v}]]);_403‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);




dotenv.config();

const sendEmail = (options) => {
  let transporter = nodemailer.createTransport(
    smtpTransport({
      service: "gmail",
      host: "smtp.gmail.com",
      auth: {
        user: process.env.SENDER_EMAIL,
        pass: process.env.PASSWORD,
      },
      tls: {
        rejectUnauthorized: false
      }
    })
  );

  let mailOptions = {
    from: "somerealemail@gmail.com",
    to: options.email,
    subject: options.subject,
    html: options.text,
  };

  transporter.sendMail(mailOptions, function(error, info) {
    if (error) {
        _403‍.g.console.log(error)
    }else {
        console.log(`Email sent ${info.response}`)
    }
  })

};

_403‍.d(sendEmail);
